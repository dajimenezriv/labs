global:
  # Lag in this lab grows by hundreds of records a second, and a rebalance
  # after a scale-out settles in a few. 1s keeps both visible.
  scrape_interval: 1s
  evaluation_interval: 1s

scrape_configs:
  - job_name: kafka
    static_configs:
      - targets: ["exporter:9200"]
